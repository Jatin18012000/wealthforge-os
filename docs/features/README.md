# Feature docs

Per-feature design notes go here once implementation begins on features
complex enough to warrant a dedicated doc beyond what `docs/0X_*.md` already
covers (e.g. a specific ingestion edge case, a specific goal-funding
algorithm detail). Empty at M0 — populated starting M3 as vertical slices
land.
