# data

The live SQLite database (`wealthforge.db`) and any uploaded file blobs live
here at runtime. This directory's contents (other than this README and
`fixtures/`, if used) are gitignored — see `.gitignore` and
`docs/13_SECURITY_PRIVACY.md`. Real financial data is never committed to
source control.
