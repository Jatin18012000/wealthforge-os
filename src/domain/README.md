# src/domain

Framework-free deterministic financial engine. No imports from `react`,
`next`, or `src/ai`. Built starting M4 per `docs/07_FINANCIAL_CALCULATIONS.md`
and `docs/20_BUILD_ROADMAP.md`. Empty at M0 by design — see
`docs/00_MASTER_PLAN.md` ("do not start with the UI" applies equally to not
building the engine before the data model under it exists).
